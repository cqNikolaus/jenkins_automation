pipeline {
  agent any
  stages {
    stage('Create Jenkins Instance') {
      steps {
        echo "create jenkins"
      }
    }
    stage('Check successfull Installation') {
      steps {
        echo "test jenkins"
      }
    }
    stage('Shutdown Jenkins Instance') {
      steps {
        echo "kill jenkins"
      }
    }
  }
}
